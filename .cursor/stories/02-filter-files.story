# 任务 2: 筛选需要审查的文件

## 目标
获取当前 PR 中需要进行人工审查的文件列表，排除自动生成的文件。

## 步骤
1.  **获取变更列表:** 执行 `git diff --name-only origin/dev...HEAD` 获取所有变更文件。
2.  **筛选文件:** 使用 `grep -v "/graphql/"` 过滤掉 `/graphql/`、`/.cursor/` 目录下的文件和 `package-lock.json` 文件。
3.  **记录列表:** 保存筛选后的文件列表以供后续步骤使用。

## 验收标准 (AC)
- 成功执行命令并获得文件列表。
- 文件列表中不包含 `/graphql/` 目录下的文件。
- 文件列表中不包含 `/.cursor/` 目录下的文件。
- 文件列表中不包含 `package-lock.json` 文件。

## 状态
[ ] 未开始
[ ] 进行中
[ ] 已完成 